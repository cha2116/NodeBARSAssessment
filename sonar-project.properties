    # required metadata
    sonar.projectKey=bars-service.charly.dalagon
    sonar.projectName=BARS Service
    sonar.projectVersion=1.0

    # optional description
    sonar.projectDescription=Scan project.

    # path to source directories (required)
     sonar.sources=./
    # sonar.java.source = 1.11

    # path to test source directories (optional)
    # sonar.tests=testDir1,testDir2
    # path to project binaries (optional), for example directory of Java bytecode
    # sonar.binaries=binDir
    # sonar.java.binaries=target
# Encoding of the source files
sonar.sourceEncoding=UTF-8
    # optional comma-separated list of paths to libraries. Only path to JAR file is supported.
    #sonar.libraries=lib/*.jar
    # The value of the property must be the key of the language.
    sonar.login=46e9bf8d84a5a52f589525311d65fcfb9ca60c50
    sonar.exclusions=node_modules/**
    sonar.language=js

    # Additional parameters

    # sonar.my.property=value
    sonar.scm.disabled=true
